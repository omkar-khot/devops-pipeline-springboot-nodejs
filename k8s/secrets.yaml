# NOTE: Secrets should be created using kubectl or external secret managers
# This is a template file for reference only
# DO NOT commit actual secrets to Git

apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: devops-app
type: Opaque
stringData:
  # Database credentials (use actual values in production)
  database-url: "postgresql://username:password@db-host:5432/dbname"
  database-username: "dbuser"
  database-password: "CHANGE_ME"
  
  # API keys (use actual values in production)
  api-key: "CHANGE_ME"
  jwt-secret: "CHANGE_ME"
  
# To create secrets securely:
# kubectl create secret generic app-secrets \
#   --from-literal=database-url='postgresql://...' \
#   --from-literal=database-password='actual-password' \
#   -n devops-app
